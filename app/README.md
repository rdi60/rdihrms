# Rajan Dental Attendance — app

React + TypeScript + Vite PWA. See the repo root `README.md` for full setup
instructions (Supabase project, schema, staff accounts, env vars).

```
npm install
npm run dev
```
